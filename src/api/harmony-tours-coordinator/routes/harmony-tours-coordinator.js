'use strict';

/**
 * harmony-tours-coordinator router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::harmony-tours-coordinator.harmony-tours-coordinator');
