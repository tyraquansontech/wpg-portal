'use strict';

/**
 * harmony-tours-coordinator service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::harmony-tours-coordinator.harmony-tours-coordinator');
